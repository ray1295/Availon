// export class Items {
//     public name: string;
//     public amount: number;

//     constructor(public name: string, public amount: number) {
//         this.name = name;
//         this.amount = amount;
//     }
// }

// #IMPORTANT# -> Upper code === as lower code

export class Items {
    constructor(public name: string, public amount: number) {
    }
}